# DarkLead Architecture

> AI-Generated by Claude Sonnet 4.6

## Overview
DarkLead is a 4-layer AI-powered code intelligence platform for automated SAST,
technical debt scoring, and LLM-assisted remediation.

## Topology

```
┌─────────────────────────────────────────────────────┐
│                   Vue 3 Frontend (Port 5173/7860)    │
│  17 Panels: Command Center, Heatmap, Issues, Fixes  │
│  Code Analyzer, Security, Compliance, Deps, AI Chat │
└─────────────────┬───────────────────────────────────┘
                  │ REST/WebSocket/SSE
┌─────────────────▼───────────────────────────────────┐
│              FastAPI Backend (Port 7860)             │
│  /api/scan  /api/report  /api/analyze/code          │
│  /api/advisories  /api/chat  /api/ws  /api/sse      │
└──────┬──────────────────────────┬────────────────────┘
       │ Celery Tasks             │ Direct (no Redis)
┌──────▼──────────┐   ┌──────────▼─────────────────────┐
│  13-Step Pipeline│   │  Background Thread Fallback     │
│  Clone→Scan→LLM  │   │  asyncio.run() in threading.Thread│
└──────┬───────────┘   └────────────────────────────────┘
       │
┌──────▼──────────────────────────────────────────────┐
│              Scanner Ensemble (19 tools)             │
│  Python: bandit ruff vulture lizard radon            │
│  Secrets: gitleaks detect-secrets env-checker        │
│  SCA: trivy pip-audit npm-audit osv-scanner          │
│  IaC: checkov hadolint tfsec                         │
│  JS/Java: eslint pmd spotbugs semgrep                │
└──────┬───────────────────────────────────────────────┘
       │
┌──────▼──────────────────────────────────────────────┐
│                  LLM Layer                           │
│  Ollama (qwen2.5-coder:14b) — local, offline         │
│  Anthropic Claude Sonnet 4.6 — cloud, production    │
│  Auto-routing: placeholder key → Ollama             │
└─────────────────────────────────────────────────────┘
```

## Debt Score Formula
```
DebtScore = 0.35 × severity_weighted
          + 0.20 × complexity_avg/30
          + 0.20 × (churn/max_churn) × (complexity/30)
          + 0.15 × dep_staleness
          + 0.10 × test_coverage_gap
```
Grade: A(0-25) B(26-45) C(46-65) D(66-80) F(>80)
